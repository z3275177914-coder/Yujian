# 玉鉴安全政策

## 报告安全问题

请不要在公开 Issue 中发布可利用的漏洞细节、用户图片、API Key、钥匙串内容、私钥或完整本机路径。

如果仓库已启用 GitHub Private Vulnerability Reporting，请通过仓库的 Security 页面提交私密报告；这是首选渠道。如果该功能尚未启用，请先提交不包含敏感细节的最小 Issue，说明需要私密沟通，不要在公开讨论中继续粘贴复现材料。

报告中可包含：受影响的版本或提交、macOS 版本、最小复现步骤、影响范围和安全修复建议。请在提交前移除图片 EXIF/GPS、访问令牌、绝对路径和其他个人信息。

## 关注范围

玉鉴重点关注以下安全边界：

- 恶意或损坏图片、GIF、动态照片触发的崩溃、越界或资源耗尽；
- 文件路径、重命名、复制、移动到废纸篓和导出覆盖行为；
- AI API Key、AI 历史、临时文件和系统剪贴板的意外泄露；
- 自定义 AI 接口和本地工具桥接绕过用户确认或安全策略；
- 从 Release 下载的应用包、校验文件或打包脚本被篡改。

当前开源包使用 ad-hoc 签名，不提供 Developer ID 身份验证或 Apple 公证。下载者应核验 Release 来源和 SHA-256；安装提示的处理边界见 [`Docs/INSTALLATION.md`](Docs/INSTALLATION.md)。

## 支持范围

当前公开支持基线为 `0.4.x` 中的最新版本。安全修复会优先针对最新源码和最新 Release；旧版本是否修复取决于问题严重程度和维护资源。玉鉴不承诺固定响应时间或跨 macOS 版本的安全行为一致性。
